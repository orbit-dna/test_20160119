<% if @student.errors.any? %>
	<div id="error_explanation">
		<h2>
			<%= @student.errors.count %> 项错误产生
		</h2>
		<ul>
			<% @student.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
